REACT_APP_NODE_ENV=production
REACT_APP_API_URL=/api
# For Azure deployment: The frontend and backend are served from the same domain
# So we use relative /api path which will resolve to https://winhronboard.azurewebsites.net/api
