global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'azure-container-apps'
    environment: 'production'

scrape_configs:
  # Backend application metrics via OTEL Collector
  - job_name: 'winonboard-backend'
    static_configs:
      - targets: ['winonboard-otel-collector.internal.gentlemeadow-8e12f1a7.westus2.azurecontainerapps.io:9464']
        labels:
          service: 'backend'
          app: 'winonboard'
    
    # For external ingress, use the external FQDN
    # - targets: [':9464']

  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          service: 'prometheus'

